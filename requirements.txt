ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=2.0.0
pandas>=2.0.0
playsound==1.2.2

# ── Face recognition whitelist ────────────────────────────────────────────────
# dlib must be installed as a pre-built binary BEFORE face-recognition.
# Run these three commands in order (do NOT use plain "pip install dlib"):
#
#   pip install dlib-bin
#   pip install face-recognition-models
#   pip install face-recognition --no-deps
#
# dlib-bin provides a pre-compiled Windows wheel so no C++ compiler is needed.
cmake>=3.25.0
dlib-bin>=19.24.0
face-recognition-models>=0.3.0
face-recognition>=1.3.0
